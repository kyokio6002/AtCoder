#include<bits/stdc++.h>
using namespace std;

using ll = long long;

int main(){

  int h,w,c;
  cin >> h >> w >> c;

  vector<vector<int>> a(h,vector<int>(w));
  for(int y=0;y<h;y++)for(int x=0;x<w;x++)cin >> a[y][x];



}
